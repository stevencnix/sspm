About SSPM
==========

Super Simple Plugin Manager - SSPM is a lightweight, hands-off Python plugin manager. Drop a
plugin's code and a small ``.info`` file describing it into a plugins directory, and SSPM
discovers, imports, categorizes, and instantiates it for you -- no extra registration step needed.
