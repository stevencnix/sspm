class PluginBase:
    """
    Marker base class for plugins. Subclass this (directly or via your own intermediate base class,
    e.g. to define a plugin category) so PluginManager can find and categorize your plugin.
    """
    pass
